export const environment = {
    production: true,
    urlBackend: 'http://backend', // URL del backend en producción (Docker)
    urlFrontend: 'http://frontend', // URL del frontend en producción (Docker)
  };